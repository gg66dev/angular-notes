// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  USER_POOL_ID: 'us-east-1_nZCwIrFzR',
  USER_POOL_DOMAIN_NAME: 'mytodolist-development-ggp.auth.us-east-1.amazoncognito.com',
  CLIENT_ID: '32qhbfl51krprbv7tqua4f0sd0',
  IDENTITY_POOL_ID: 'us-east-1:89cdace7-b3d6-450e-bfcc-4d2aa07916fa',
  REGION: 'us-east-1',
  WEB_REDIRECT_URI: 'http://localhost:4200'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.



